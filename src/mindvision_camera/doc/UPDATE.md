# 更新日志

## 第一次提交

现在有利用参数的两个回调函数实现的运行时赋参  
这个参数和类的成员变量交互了，但还没有和相机SDK（也就相当于是相机硬件）交互  
现在没有利用多线程执行器，同时没有做进程内通信  
但也可以写写现在性能的测试函数  
同时CMakeList的写法之后要做修改  

能够死频率取相机图像并发布  
结束相机能够正常退出  

## 第二次提交

现在能运行时通过命令行直接设置相机曝光时间和节点取图频率  
